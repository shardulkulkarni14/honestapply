---
name: Bug report
about: Something isn't working
labels: bug
---

**What happened**
A clear description of the bug.

**To reproduce**
Steps / command(s). If it's pipeline-related, the stage (`discover` / `enrich` / `score` /
`tailor` / `cover-letter` / `apply`) and whether `honestapply init && honestapply simulate` also shows it.

**Expected behaviour**

**Environment**
- OS:
- Python version:
- honestapply version / commit:
- LLM provider (`stub` / `claude_cli` / `anthropic` / …):

**Logs / output**
Paste relevant output. ⚠️ **Redact any personal data** (your name, email, résumé content,
real company/application data) before posting.
